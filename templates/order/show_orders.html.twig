{% extends 'base.html.twig' %}

{% block title %}Allez, courage les gars !{% endblock %}

{% block body %}
   {{ path('order_show') ? ('<body class="background-show-img">') : '' }}

   <h1 class="title-show-orders">LISTE DES COMMANDES</h1>
<div class="container-fluid mt-5">
   <div  class="row">
   {% for order in orders %}
         <div class="col-md-3 col-lg-3 col-sm-12">
      <div class="card text-center" style="width:100%; height: 15em">
         <div class="card-body">
            <h5 class="card-title">N°{{ order.id }}</h5>
         </div>
            <h3 class="card-name">{{ order.name }}</h3>
         <ul class="list-group list-group-flush">
   {% for dish in order.dishes %}
            <li class="list-group-item">{{ dish.name }} </li>
            <li class="list-group-item">{{ order.quantity }}</li>
            <li class="list-group-item text-danger">{{ order.comment }}</li>
            <li class="list-group-item text-success">{{ order.time }} </li>
      {% endfor %}
         </ul>
         {{ include('order/delete_order.html.twig') }}
      </div>

         </div>
   {% endfor %}
   </div>
</div>
{% endblock %}



